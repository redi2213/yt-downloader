name: Download Video

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube URL'
        required: true
      format_id:
        description: 'Exact format id (no fallback)'
        required: true
      audio_only:
        description: 'true for MP3'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
      - name: Install ffmpeg
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ffmpeg
          ffmpeg -version

      - name: Install Node.js (JS runtime for EJS solver)
        run: |
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs

      - name: Install yt-dlp via pip (includes EJS scripts)
        run: |
          python3 -m pip install --break-system-packages -U "yt-dlp[default]"
          yt-dlp --version

      - name: Write yt-dlp config (enable node runtime + EJS auto-fetch)
        run: |
          mkdir -p ~/.config/yt-dlp
          cat > ~/.config/yt-dlp/config <<EOF
          --js-runtimes node
          --remote-components ejs:github
          EOF

      - name: Write cookies
        run: echo "${{ secrets.YTDLP_COOKIES }}" > cookies.txt

      - name: Download video
        id: dl
        run: |
          set -x
          OUT="%(title).120s.%(ext)s"
          ARIA="aria2c:-x16 -s16 -k4M --file-allocation=none"
          CLIENT="youtube:player_client=android"

          if [ "${{ github.event.inputs.audio_only }}" = "true" ]; then
            yt-dlp --cookies cookies.txt \
              --extractor-args "$CLIENT" \
              --retries 10 --retry-sleep 30 \
              -f "bestaudio" \
              --downloader aria2c --downloader-args "$ARIA" \
              -x --audio-format mp3 \
              -o "$OUT" \
              --print filename \
              "${{ github.event.inputs.video_url }}" | tee filename.txt
            QUALITY="audio"
          else
            yt-dlp --cookies cookies.txt \
              --extractor-args "$CLIENT" \
              --retries 10 --retry-sleep 30 \
              -f "${{ github.event.inputs.format_id }}/best" \
              --downloader aria2c --downloader-args "$ARIA" \
              -o "$OUT" \
              --print filename \
              "${{ github.event.inputs.video_url }}" | tee filename.txt
            QUALITY="${{ github.event.inputs.format_id }}"
          fi

          FILE=$(tail -n1 filename.txt)
          echo "Detected file: $FILE"
          ls -la

          if [ ! -f "$FILE" ]; then
            echo "::error::Downloaded file not found: $FILE"
            exit 1
          fi

          BASE="${FILE%.*}"
          EXT="${FILE##*.}"
          FINAL="${BASE}_${QUALITY}.${EXT}"

          mv "$FILE" "$FINAL"
          echo "filename=$FINAL" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-video
          path: ${{ steps.dl.outputs.filename }}

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: run-${{ github.run_id }}
          files: ${{ steps.dl.outputs.filename }}
          prerelease: true
